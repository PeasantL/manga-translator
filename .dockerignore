.git
.venv
__pycache__
*.pyc

# Model weights live in a volume, not the image: they are ~258 MB and would
# otherwise be re-baked into every layer rebuild.
models

# Working folders. The container is handed a chapter per job.
input
output
runs

examples
