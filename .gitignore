runs
*/__pycache__/*
*.pyc
.DS_Store
datasets/*
*.pt
*.pth

# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

.DS_Store
.env.local
.idea
manga-translator-service-account-data.json
exa/*
local/*
trans-test/*
*.env
.venv
.vscode/
models/*
# Working folders. The folders themselves are tracked so a fresh clone has
# somewhere to put pages; their contents are not.
input/*
!input/README.md
output/*
!output/.gitkeep

# Weights that manga-ocr and LaMa download for themselves on first run. Bound
# into the container as a volume so a rebuild does not re-fetch them.
cache/

# Scratch for the chapter service.py has in flight.
jobs/
