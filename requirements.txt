# Core runtime
numpy>=1.26.2,<2.0
opencv-python-headless>=4.8.1.78,<5.0
# Capped below 10.0 by simple-lama-inpainting, which pins pillow<10.0.0.
pillow>=9.5.0,<10.0
requests>=2.31.0,<3.0
aiohttp>=3.9.0,<4.0
python-dotenv>=1.0.0,<2.0
tornado>=6.3.3,<7.0
tqdm>=4.66.1,<5.0

# Torch stack. Declared explicitly rather than relied on as an ultralytics
# transitive. For CUDA builds install these first from the PyTorch index, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0,<3.0
torchvision>=0.15.0,<1.0

# Detection / segmentation / inpainting
ultralytics>=8.0.218,<9.0
simple-lama-inpainting>=0.1.2,<1.0
largestinteriorrectangle>=0.2.0,<1.0
timm>=0.9.12,<1.0
vit-pytorch>=1.6.5,<2.0

# OCR
easyocr>=1.7.1,<2.0
pytesseract>=0.3.10,<1.0
transformers>=4.35.2,<5.0
sentencepiece>=0.1.99,<1.0
fugashi>=1.3.0,<2.0
unidic-lite>=1.0.8,<2.0
jaconv>=0.3.4,<1.0

# Translation
sacremoses>=0.1.1,<1.0
openai>=1.6.0,<2.0

# Text layout
pyhyphen>=4.0.3,<5.0
pycountry>=22.3.5,<23.0

# Optional: debug image viewer (translator.utils.display_image). Needs tkinter
# (apt install python3-tk). Capped below 5.0 because 5.x changed the API and
# added a license-key prompt; the code falls back to writing a PNG without it.
pysimplegui>=4.60.5,<5.0

# Datasets / synthetic training data
faker>=20.1.0,<21.0
