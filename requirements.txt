# Core runtime
numpy>=1.26.2,<2.0
# Headless because nothing here opens a window, and the full build wants libGL.
# Note that ultralytics depends on plain opencv-python and will pull it in
# anyway; the two unpack into the same cv2 directory and the full one wins the
# import. Removing it afterwards takes the module with it, so an environment
# that must stay headless has to uninstall both and reinstall this one. The
# Dockerfile does exactly that.
opencv-python-headless>=4.8.1.78,<5.0
# Capped below 11.0 rather than pinned: nothing here uses an API 10.x removed,
# and the inpainting library that used to hold this at 9.x is gone.
pillow>=9.5.0,<11.0
requests>=2.31.0,<3.0
python-dotenv>=1.0.0,<2.0

# service.py, the job API. python-multipart backs its file upload; FastAPI
# raises at import without it.
fastapi>=0.115.0,<1.0
uvicorn>=0.32.0,<1.0
python-multipart>=0.0.9,<1.0

# Torch stack. Declared explicitly rather than relied on as an ultralytics
# transitive. For CUDA builds install these first from the PyTorch index, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0,<3.0
torchvision>=0.15.0,<1.0

# Detection / segmentation / inpainting. The detector is an RT-DETR-v2 that
# transformers loads; ultralytics is here for the segmenter alone; the LaMa
# checkpoint is torchscript and needs nothing but torch to run.
ultralytics>=8.0.218,<9.0
largestinteriorrectangle>=0.2.0,<1.0

# Every model is fetched from the hub on first use and cached in HF_HOME.
huggingface_hub>=0.26.0,<1.0

# OCR, and the detector. The floor is set by the OCR model, which brings its own
# modelling code along with its weights and is written against this generation
# of the library; RT-DETR-v2, which the detector is, needs 4.49 or newer. einops
# is what that model code imports, and sentencepiece and protobuf back its
# tokenizer, which is stored in sentencepiece's format.
transformers>=4.57.0,<5.0
einops>=0.7.0,<1.0
sentencepiece>=0.1.99,<1.0
protobuf>=4.25.0,<7.0

# Translation. DeepSeek serves an OpenAI-compatible API, so the openai client
# talks to it.
openai>=1.6.0,<2.0

# Text layout
pyhyphen>=4.0.3,<5.0

# Optional: debug image viewer (translator.utils.display_image). Needs tkinter
# (apt install python3-tk). Capped below 5.0 because 5.x changed the API and
# added a license-key prompt; the code falls back to writing a PNG without it.
pysimplegui>=4.60.5,<5.0

# Font handling. Read to find out which characters a font can actually draw,
# so a symbol it lacks is drawn from one that has it instead of as an empty box.
fonttools>=4.40.0,<5.0
